require('debugs/init');
const assert = require('chai').assert;

describe('coinone-sdk', () => {
  // api
  it.skip('fail without config', () => {

  })
})
describe('order', () => {
  // scale
  it.skip('fail with invalid min qty', () => {

  })
})
describe('bin/co', () => {
  // command test
  it.skip('should message when invalid currency given', () => {

  })
})